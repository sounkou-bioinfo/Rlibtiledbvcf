% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_interface.R
\name{tiledb_vcf_store}
\alias{tiledb_vcf_store}
\title{Store VCF Files in TileDB-VCF Dataset}
\usage{
tiledb_vcf_store(
  uri,
  vcf_files = NULL,
  samples_file = NULL,
  threads = 4,
  memory_budget_mb = 2048,
  sample_batch_size = 10,
  scratch_dir = NULL,
  echo = TRUE,
  print_command = FALSE,
  ...
)
}
\arguments{
\item{uri}{Character string specifying the dataset URI (required)}

\item{vcf_files}{Character vector of VCF file paths to ingest}

\item{samples_file}{Character string, file with VCF paths (one per line)}

\item{threads}{Integer, number of threads (default: 4)}

\item{memory_budget_mb}{Integer, total memory budget in MB (default: 2048)}

\item{sample_batch_size}{Integer, samples per batch (default: 10)}

\item{scratch_dir}{Character string, directory for temporary files}

\item{echo}{Logical, whether to echo output to console (default: TRUE)}

\item{print_command}{Logical, whether to print the command being run (default: FALSE)}

\item{...}{Additional arguments passed as --key=value pairs}
}
\value{
List with command execution results
}
\description{
Ingest VCF files into an existing TileDB-VCF dataset.
}
\examples{
\dontrun{
# Store single VCF file
tiledb_vcf_store("my_dataset", "sample1.vcf.gz")

# Store multiple VCF files
tiledb_vcf_store("my_dataset", c("sample1.vcf.gz", "sample2.vcf.gz"))

# Store with custom settings
tiledb_vcf_store("my_dataset", "sample1.vcf.gz",
                 threads = 8, memory_budget_mb = 4096)
}
}
