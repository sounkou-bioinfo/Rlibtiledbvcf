% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_interface.R
\name{tiledb_vcf_export}
\alias{tiledb_vcf_export}
\title{Export Data from TileDB-VCF Dataset}
\usage{
tiledb_vcf_export(
  uri,
  output_format = "v",
  output_path = NULL,
  regions = NULL,
  regions_file = NULL,
  sample_names = NULL,
  samples_file = NULL,
  tsv_fields = NULL,
  limit = NULL,
  merge = FALSE,
  memory_budget_mb = 2048,
  echo = TRUE,
  print_command = FALSE,
  ...
)
}
\arguments{
\item{uri}{Character string specifying the dataset URI (required)}

\item{output_format}{Character string, output format: "b" (BCF), "v" (VCF), "z" (VCF.gz), "t" (TSV) (default: "v")}

\item{output_path}{Character string, output file path}

\item{regions}{Character vector of regions in format "chr:start-end"}

\item{regions_file}{Character string, BED file with regions}

\item{sample_names}{Character vector of sample names to export}

\item{samples_file}{Character string, file with sample names (one per line)}

\item{tsv_fields}{Character vector of TSV fields to export (for TSV format)}

\item{limit}{Integer, maximum number of records to export}

\item{merge}{Logical, export combined VCF file (default: FALSE)}

\item{memory_budget_mb}{Integer, memory budget in MB (default: 2048)}

\item{echo}{Logical, whether to echo output to console (default: TRUE)}

\item{print_command}{Logical, whether to print the command being run (default: FALSE)}

\item{...}{Additional arguments passed as --key=value pairs}
}
\value{
List with command execution results
}
\description{
Export variant data from a TileDB-VCF dataset.
}
\examples{
\dontrun{
# Export all data as VCF
tiledb_vcf_export("my_dataset", output_path = "output.vcf")

# Export specific region and samples
tiledb_vcf_export("my_dataset",
                  regions = "chr1:1000000-2000000",
                  sample_names = c("sample1", "sample2"),
                  output_path = "region.vcf")

# Export as TSV with specific fields
tiledb_vcf_export("my_dataset",
                  output_format = "t",
                  tsv_fields = c("SAMPLE", "POS", "REF", "ALT"),
                  output_path = "variants.tsv")
}
}
