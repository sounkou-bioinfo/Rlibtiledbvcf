% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli_interface.R
\name{tiledb_vcf_create}
\alias{tiledb_vcf_create}
\title{Create TileDB-VCF Dataset}
\usage{
tiledb_vcf_create(
  uri,
  attributes = NULL,
  vcf_attributes = NULL,
  anchor_gap = 1000,
  no_duplicates = FALSE,
  tile_capacity = 10000,
  checksum = "sha256",
  echo = FALSE,
  print_command = FALSE,
  ...
)
}
\arguments{
\item{uri}{Character string specifying the dataset URI (required)}

\item{attributes}{Character vector of INFO/FORMAT field names to store as separate attributes}

\item{vcf_attributes}{Character string with VCF file to use for extracting all INFO/FORMAT fields}

\item{anchor_gap}{Integer, anchor gap size (default: 1000)}

\item{no_duplicates}{Logical, allow duplicate start positions (default: FALSE)}

\item{tile_capacity}{Integer, tile capacity for array schema (default: 10000)}

\item{checksum}{Character string, checksum type: "md5", "sha256", or "none" (default: "sha256")}

\item{echo}{Logical, whether to echo output to console (default: FALSE)}

\item{print_command}{Logical, whether to print the command being run (default: FALSE)}

\item{...}{Additional arguments passed as --key=value pairs}
}
\value{
List with command execution results
}
\description{
Create an empty TileDB-VCF dataset at the specified URI.
}
\examples{
\dontrun{
# Create a basic dataset
tiledb_vcf_create("my_dataset")

# Create with specific attributes
tiledb_vcf_create("my_dataset",
                  attributes = c("info_AF", "fmt_GT", "fmt_DP"))
}
}
